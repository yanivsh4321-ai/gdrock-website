# === GDRock — never deploy these to the public site ===

# Prospect/customer data (PII) — must never be committed or served
leads.csv
shopify_leads.csv
outreach_drafts/
outreach_drafts.csv
sent_log.csv
sweep.log
*.output

# Local lead-gen / outreach tooling (internal only)
leadfinder.py
shopify_audit.py
make_emails.py
zoho_send.py
zoho_check.py
zoho_invoice.py
lead.js

# Caches & local env
__pycache__/
*.pyc
.env
.env.*
.claude/
node_modules/

# Local tooling / generated caches — never commit
.wrangler/
graphify-out/
kickbacks_inspect/
kickbacks.vsix
.mcp.json
gdrock-banner/

# Internal outreach/automation tooling — local only
digest_send.py
digest_body.txt
gdrock_log.py
reply_watch.py
send_schedule.py
scratch_write_csv.py
reply_seen.json

# Before-consent verification rig (internal) + its evidence output (prospect data)
verify_consent.js
verify_blocker.js
verification/
package.json
package-lock.json

# Outreach senders + drafts + logs (internal, prospect data — never deploy)
send_batch3.py
send_day*_batch2.py
send_nudge1_batch1.py
send_nudge2_researchweek.py
outreach_drafts_batch*.csv
day1_send.log
missim/

# Nudge cadence engine + its state/drafts (internal, prospect data — never deploy)
nudge_cadence.py
nudge_state.csv
nudge_drafts/
loom_scripts/
make_loom_script.py
rerun_all_log.txt
rerun_batch4_log.txt

# Skill tooling state — local only, not site content
.agents/
.impeccable/
skills-lock.json
